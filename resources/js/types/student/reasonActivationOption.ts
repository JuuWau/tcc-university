import {
        type StudentActivationReasonKey,
} from '@/constants/studentActivationReason';

export type ReasonActivateOption = {
        value: StudentActivationReasonKey;
        label: string;
};